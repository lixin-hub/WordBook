<?xml version="1.0" encoding="utf-8"?>
<!-- 定义矩形rectangle -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:dither="false"
    android:innerRadius="@android:dimen/dialog_min_width_minor"
    android:innerRadiusRatio="@android:dimen/app_icon_size"
    android:shape="rectangle"
    android:tintMode="multiply"
    android:useLevel="true"
    android:visible="true">
    <!-- 设置边框的大小和颜色 -->
    <stroke
        android:width="1dip"
        android:color="@color/colorTextBackground" />

    <!-- 设置矩形内的颜色,此处为透明色 -->
    <solid android:color="@color/colorTextBackgroundSelected" />


    <!-- 定义圆角弧度 -->
    <corners
        android:bottomLeftRadius="5dp"
        android:bottomRightRadius="5dp"
        android:topLeftRadius="5dp"
        android:topRightRadius="5dp" />

</shape>